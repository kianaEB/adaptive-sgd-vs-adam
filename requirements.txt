numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
# >=2.21: the first release with a native-Windows CPython 3.13 wheel. Older
# floors (e.g. 2.20) resolve to a cp39-only Windows wheel that cannot import
# under 3.13 ("DLL load failed" / missing python39.dll).
tensorflow>=2.21
matplotlib>=3.7
pytest>=7.4
