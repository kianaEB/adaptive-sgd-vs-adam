[pytest]
testpaths = tests
python_files = test_*.py
# Keep pytest's tmp under the project: this machine's system temp holds a
# permission-locked pytest-of-<user> dir that breaks the default tmp cleanup.
addopts = -q --basetemp=.pytest_tmp
